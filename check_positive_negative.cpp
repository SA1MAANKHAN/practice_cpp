#include<iostream>
using namespace std;
int main()
{
    int a;
    cout<<"enter the num";
    cin>>a;
    if(a==0)
    cout<<"num is zero";
    else if(a<0)
    cout<<"num is negative";
    else
    cout<<"num is positive";
    return 0;
}
